#include <stdio.h>
#include <math.h>

/* 
 * Author: Noah Poling
 * Title: derivative.h
 * Description: header file for functions for handling derivatives
 */

//Default parameters if value not specified

double * constant = 1;
double * exponent = 1;

// Rules:
// Muliplication by constant (if term is constant value then return 0)

double constantDerivative(double constant,): {
  return 0;
}
const char* powerRule(double constant, double exponent) {
  if (exponent = 1) {
    return constant
  }
}
